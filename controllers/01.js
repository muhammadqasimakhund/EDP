export const homeControllers = (req,res)=>{
    res.send('Welcome to Home Page')
}

export const aboutControllers = (req,res)=>{
    res.send('Welcome to About Page')
}

export const blogControllers = (req,res)=>{
    res.send('Welcome to Blog Page')
}